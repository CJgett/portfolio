export const metadata = {
  title: "Projects | Carly Gettinger",
  description: "Carly Gettinger's web development projects.",
};

export default function ProjectsLayout({ children }) {
  return children;
}
