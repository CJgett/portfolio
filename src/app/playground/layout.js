export const metadata = {
  title: "Pointillist Playground",
  description: "Upload an image and transform it into a pointillist artwork.",
};

export default function PlaygroundLayout({ children }) {
  return children;
}
